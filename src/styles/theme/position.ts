import { css } from "styled-components";

const position = css`
  .fixed {
    position: fixed;
  }
  .relative {
    position: relative;
  }
  .absolute {
    position: absolute;
  }
`;

export default position;
