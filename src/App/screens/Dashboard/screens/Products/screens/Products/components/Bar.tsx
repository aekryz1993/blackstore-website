import { BarContainer, BarTitle } from "../../../style";

const Bar = () => (
  <BarContainer className="flex justify-center items-center w-full py-2 lg__w-4xl">
    <BarTitle>CODES</BarTitle>
  </BarContainer>
);

export default Bar;
