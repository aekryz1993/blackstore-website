import { Button } from "./Button";
import { HeaderText } from "./HeaderText";
import { Item } from "./Item";
import { ErrorText, TextButton, LoadingIcon } from "./Text";

const components = {
  Button,
  HeaderText,
  ErrorText,
  TextButton,
  LoadingIcon,
  Item,
};

export default components;
